import Ember from 'ember';

export default Ember.Route.extend({

  // setup: Ember.on('didInsertElement', function() {
  //   Ember.$('body').on('keyup', (e) => {
  //       this.set('valueFor', this.get('value'));
  //   });
  // }),


});
